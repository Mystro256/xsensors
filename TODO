For 0.80:
- Gnome AppMenu(gtk3)/Gtkmenubar(gtk2):
  > Preferences:
    - Change "theme" (default or select, with preview)
    - Change sensors config file (default or select)
    - Celsius (Default) or Fahrenheit
    - Update time (default, do not update, or select)
Low Priority:
- Better range readouts (neat speedometer type possibly)
- Port gtkbox to gtkgrid (probably deprecated in gtk4)
